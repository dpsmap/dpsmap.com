$(function() {
  
  // contact form animations
  $('#contact').click(function() {
    $('#contactForm').fadeToggle();
  })
  $(document).mouseup(function (e) {
    var container = $("#contactForm");

    if (!container.is(e.target) 
        && container.has(e.target).length === 0) 
    {
        container.fadeOut();
    }
  });
  $('#contact1').click(function() {
    $('#contactForm1').fadeToggle();
  })
  $(document).mouseup(function (m) {
    var container = $("#contactForm1");

    if (!container.is(m.target) 
        && container.has(m.target).length === 0) 
    {
        container.fadeOut();
    }
  });
  $('#contact2').click(function() {
    $('#contactForm2').fadeToggle();
  })
  $(document).mouseup(function (y) {
    var container = $("#contactForm2");

    if (!container.is(y.target) 
        && container.has(y.target).length === 0) 
    {
        container.fadeOut();
    }
  });
  $('#contact3').click(function() {
    $('#contactForm3').fadeToggle();
  })
  $(document).mouseup(function (a) {
    var container = $("#contactForm3");

    if (!container.is(a.target) 
        && container.has(a.target).length === 0) 
    {
        container.fadeOut();
    }
  });
  $('#contact4').click(function() {
    $('#contactForm4').fadeToggle();
  })
  $(document).mouseup(function (k) {
    var container = $("#contactForm4");

    if (!container.is(k.target) 
        && container.has(k.target).length === 0) 
    {
        container.fadeOut();
    }
  });
   $('#contact5').click(function() {
    $('#contactForm5').fadeToggle();
  })
  $(document).mouseup(function (s) {
    var container = $("#contactForm5");

    if (!container.is(s.target) 
        && container.has(s.target).length === 0) 
    {
        container.fadeOut();
    }
  }); 
  $('#contact6').click(function() {
    $('#contactForm6').fadeToggle();
  })
  $(document).mouseup(function (mm) {
    var container = $("#contactForm6");

    if (!container.is(mm.target) 
        && container.has(mm.target).length === 0) 
    {
        container.fadeOut();
    }
  });
  $('#contact7').click(function() {
    $('#contactForm7').fadeToggle();
  })
  $(document).mouseup(function (sh) {
    var container = $("#contactForm7");

    if (!container.is(sh.target) 
        && container.has(sh.target).length === 0) 
    {
        container.fadeOut();
    }
  });
  $('#contact8').click(function() {
    $('#contactForm8').fadeToggle();
  })
  $(document).mouseup(function (man) {
    var container = $("#contactForm8");

    if (!container.is(man.target) 
        && container.has(man.target).length === 0) 
    {
        container.fadeOut();
    }
  });
  $('#contact9').click(function() {
    $('#contactForm9').fadeToggle();
  })
  $(document).mouseup(function (mon) {
    var container = $("#contactForm9");

    if (!container.is(mon.target) 
        && container.has(mon.target).length === 0) 
    {
        container.fadeOut();
    }
  });
  $('#contact10').click(function() {
    $('#contactForm10').fadeToggle();
  })
  $(document).mouseup(function (kay) {
    var container = $("#contactForm10");

    if (!container.is(kay.target) 
        && container.has(kay.target).length === 0) 
    {
        container.fadeOut();
    }
  });
  
});

